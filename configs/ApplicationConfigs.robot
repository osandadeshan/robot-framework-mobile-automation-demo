*** Variables ***
# Number of retries
${SMALL_RETRY_COUNT}           2x
${MEDIUM_RETRY_COUNT}          3x
${LARGE_RETRY_COUNT}           5x

# Delay for a retry
${RETRY_DELAY}                 1 s

# Timeout
${TIMEOUT}                     60
